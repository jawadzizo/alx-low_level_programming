#include <stdio.h>
#include "main.h"

/**
 * mul - a function to multiply 2 numbers
 * @a: number a
 * @b: number b
 * Return: returns the value of the multiplication
 */

int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
